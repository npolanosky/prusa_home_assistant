{
  "config": {
    "abort": {
      "already_configured": "This printer is already configured"
    },
    "error": {
      "invalid_auth": "Authentication failed — check your API key or token. Enable debug logging for details (see description below).",
      "cannot_connect": "Unable to connect. Check your network and enable debug logging for details.",
      "no_printers": "Connected successfully but no printers were found. Make sure your printer is registered in Prusa Connect.",
      "printer_not_found": "Selected printer could not be found."
    },
    "step": {
      "user": {
        "title": "Prusa Connect",
        "description": "Choose how to connect to your Prusa printer.",
        "data": {
          "connection_type": "Connection method"
        }
      },
      "local": {
        "title": "PrusaLink (Local Connection)",
        "description": "Connect directly to your printer on the local network.\n\nFind the API key in your printer's web interface at **http://your-printer-ip** → Settings → API Key.\n\nFor debug logs, add this to `configuration.yaml`:\n```\nlogger:\n  logs:\n    custom_components.prusa_connect: debug\n```",
        "data": {
          "host": "Printer IP address or hostname (e.g. 192.168.1.100)",
          "api_key": "API Key",
          "printer_name": "Printer name (optional, auto-detected if blank)"
        }
      },
      "cloud": {
        "title": "Prusa Connect (Cloud)",
        "description": "Connect via the Prusa Connect cloud at [{prusa_connect_url}]({prusa_connect_url}).\n\nThis requires an **OAuth2 access token** from your Prusa Account (the same account used by PrusaSlicer's cloud features). This is NOT the same as the local PrusaLink API key shown in your printer's web interface.\n\n**Note:** If you use an API key from OrcaSlicer or your printer's settings page, choose **Local (PrusaLink)** instead and enter your printer's IP address.\n\nFor debug logs, add to `configuration.yaml`:\n```\nlogger:\n  logs:\n    custom_components.prusa_connect: debug\n```",
        "data": {
          "api_key": "Access Token"
        }
      },
      "select_printer": {
        "title": "Select Printer",
        "description": "Choose which printer to add to Home Assistant.",
        "data": {
          "printer_uuid": "Printer"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Prusa Connect Options",
        "data": {
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}
